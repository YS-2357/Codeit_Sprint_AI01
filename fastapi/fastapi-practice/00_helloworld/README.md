## extension

- postman
- sqlite
  - ctrl + shift + p: sqlite: open database
- simple browser multi

## 기타명령어

- pip install uv
- uv init -p 3.10 # 파이썬 버전
- uv add fastapi (.venv생성)

- uv run uvicorn main:app --reload
- uv run --isolated uvicorn main:app --reload (만약 패키지가 없다면 외부 파이썬 참조하는 경우가 있는데 그렇게 동작하지 않도록)
- curl.exe -X GET http://127.0.0.1:8000
  
## etc

- git copilot disable
